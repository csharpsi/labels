<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
      {{#if currentUser}}
      <div>Welcome back, {{currentUser.firstName}}</div>
      {{/if}}
    {{{body}}}
  </body>
</html>
